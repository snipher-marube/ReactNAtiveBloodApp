import React from 'react';
import {StyleSheet, View, Text} from 'react-native';

const Card = () => {
    return (
        <View>
            <Text>Card</Text>
        </View>
    )
};

export default Card;

const styles = StyleSheet.create({

})